#!/usr/bin/env sh

# Usage:
# ./bootstrap-pi <pi ip address>

echo Type "raspberry" when prompted for passwords.
ssh pi@$1 "mkdir -p ~/.ssh"
cat ~/.ssh/id_rsa.pub | ssh pi@$1 "cat > ~/.ssh/authorized_keys"
